import 'package:flutter/material.dart';

class BlocErrorState extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Bad Internet Connection"),
      ),
    );
  }
}
